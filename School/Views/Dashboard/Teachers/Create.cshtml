@model School.Models.Teacher

@{
    ViewData["Title"] = Localizer["create"];
}

<h1>@ViewData["Title"]</h1>

<h4>@Localizer["teacher"]</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-controller="Dashboard" asp-action="CreateTeacher" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="Name" class="control-label">@Localizer["name"]</label>
                <input asp-for="Name" class="form-control" />
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Qualification" class="control-label">@Localizer["qual"]</label>
                <input asp-for="Qualification" class="form-control" />
                <span asp-validation-for="Qualification" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="FotoPath" class="control-label">@Localizer["photo"]</label>
                <input asp-for="FotoPath" class="form-control" />
                <span asp-validation-for="FotoPath" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="IdentityUserId" class="control-label">
                    @Localizer["user"]
                </label>

                <select asp-for="IdentityUserId"
                        class="form-control"
                        asp-items="ViewBag.IdentityUserId">
                    <option value="">-- @Localizer["select_user"] --</option>
                </select>

                <span asp-validation-for="IdentityUserId" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Add" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-controller="Dashboard" asp-action="Teachers">@Localizer["back_to_list"]</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}