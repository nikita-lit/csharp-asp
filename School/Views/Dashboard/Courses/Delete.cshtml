@model School.Models.Course

@{
    ViewData["Title"] = Localizer["delete"];
}

<h1>@ViewData["Title"]</h1>

<h4>@Localizer["сourse"]</h4>
<hr />

<div class="alert alert-warning">
    @if ((bool?)ViewData["HasTrainings"] == true)
    {
        <p>@Localizer["cannot_delete_has_trainings", ViewData["TrainingsCount"]]</p>
    }
    else
    {
        <p>@Localizer["confirm_delete"]</p>
    }
    <dl class="row">
        <dt class="col-sm-2">@Localizer["name"]</dt>
        <dd class="col-sm-10">@Model.Name</dd>

        <dt class="col-sm-2">@Localizer["level"]</dt>
        <dd class="col-sm-10">@Model.Level</dd>

        <dt class="col-sm-2">@Localizer["description"]</dt>
        <dd class="col-sm-10">@Model.Description</dd>
    </dl>

    @if (!((bool?)ViewData["HasTrainings"] == true))
    {
        <form asp-controller="Dashboard" asp-action="DeleteCourseConfirmed" method="post">
            <input type="hidden" name="id" value="@Model.Id" />
            <button type="submit" class="btn btn-danger">@Localizer["delete"]</button>
            <a asp-controller="Dashboard" asp-action="Courses" class="btn btn-secondary">@Localizer["cancel"]</a>
        </form>
    }
    else
    {
        <a asp-controller="Dashboard" asp-action="Courses" class="btn btn-secondary">@Localizer["back_to_list"]</a>
    }
</div>
