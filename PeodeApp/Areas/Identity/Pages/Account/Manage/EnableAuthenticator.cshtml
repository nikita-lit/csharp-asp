@page
@model EnableAuthenticatorModel
@{
    ViewData["Title"] = "Seadista autentimisrakendus";
    ViewData["ActivePage"] = ManageNavPages.TwoFactorAuthentication;
}

<partial name="_StatusMessage" for="StatusMessage" />
<h3>@ViewData["Title"]</h3>
<div>
    <p>Autentimisrakenduse kasutamiseks järgige järgmisi samme:</p>
    <ol class="list">
        <li>
            <p>
                Laadige alla kahefaktoriline autentimisrakendus, näiteks Microsoft Authenticator (Android/iOS) või Google Authenticator (Android/iOS).
            </p>
        </li>
        <li>
            <p>Skannige QR-kood või sisestage see võti <kbd>@Model.SharedKey</kbd> oma autentimisrakendusse. Tühikud ja suurtähed ei loe.</p>
            <div class="alert alert-info">Lisateave: <a href="https://go.microsoft.com/fwlink/?Linkid=852423">kuidas lubada QR-koodi genereerimist</a>.</div>
            <div id="qrCode"></div>
            <div id="qrCodeData" data-url="@Model.AuthenticatorUri"></div>
        </li>
        <li>
            <p>
                Kui olete QR-koodi skanninud või ülaltoodud võtme sisestanud, annab teie autentimisrakendus unikaalse koodi. Sisestage see kinnitusvälja allpool.
            </p>
            <div class="row">
                <div class="col-md-6">
                    <form id="send-code" method="post">
                        <div class="form-floating mb-3">
                                    <input asp-for="Input.Code" class="form-control" autocomplete="off" placeholder="Sisestage kood."/>
                                    <label asp-for="Input.Code" class="control-label form-label">Kinnituskood</label>
                                    <span asp-validation-for="Input.Code" class="text-danger"></span>
                                </div>
                                <button type="submit" class="w-100 btn btn-lg btn-primary">Kinnita</button>
                        <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>
                    </form>
                </div>
            </div>
        </li>
    </ol>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
